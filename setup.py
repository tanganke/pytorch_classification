from setuptools import setup

setup(
    name="pytorch_classification",
    version="0.0.1",
    author="tanganke",
    packages=["pytorch_classification"],
    install_requires=[
        "matplotlib",
        "scikit-learn",
        "torchshow",
    ],
)
